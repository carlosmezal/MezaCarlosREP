# Reto Semana 05 - Perfilador de Datasets

Herramienta en Python que analiza archivos CSV y genera un reporte de calidad de datos por columna.

python main.py --input data/ventas.csv --output outputs/perfil_ventas.csv